# Auditoria do projeto

Registo cumulativo de achados de auditorias (segurança, qualidade, dívida técnica). Cada achado fica marcado como `[ ]` (por resolver), `[~]` (correção pronta no repo mas a precisar de um passo manual fora dele) ou `[x]` (corrigido, com data e commit/PR se aplicável). Não apagar achados corrigidos — manter o histórico.

## 31/08/2026

- [~] **Médio** (config pronta em 31/08/2026, **falta aplicar na VM**) — a SPA servida pelo nginx não devolve **nenhum** cabeçalho de segurança. Verificado em produção: `curl -I https://carrinhodecompras.pt/` não traz CSP, `X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy` nem HSTS, enquanto `curl -I https://carrinhodecompras.pt/api/health` traz o conjunto completo do helmet. O helmet só cobre as respostas do Fastify (`/api/*`) — o HTML e o JS da app não passam por lá. Como o JWT vive em `localStorage` (`frontend/src/stores/auth.ts:8`), um XSS na SPA dá tomada de conta completa sem nenhuma CSP a mitigar; e sem `frame-ancestors`/`X-Frame-Options` a app pode ser embebida num iframe (clickjacking). **Correção preparada:** `nginx.conf` (no repo) passou a ter CSP, `X-Frame-Options: DENY`, `X-Content-Type-Options`, `Referrer-Policy`, `Permissions-Policy` e HSTS, no `location /` (fora do `/api/`, para não duplicar os do helmet). A CSP **não** usa `'unsafe-inline'` em `script-src` — para isso ser possível, o arranque do Google Analytics saiu do `index.html` para `frontend/public/analytics.js`. `style-src` mantém `'unsafe-inline'`, inevitável com o Vue/reka-ui a posicionarem-se por estilo inline. Validada localmente a servir o `dist/` com estes cabeçalhos: login, tabelas, dialogs, combobox e Select exercitados com **zero** `securitypolicyviolation` e a fonte Inter a carregar. **Falta o passo manual na VM**: copiar o `nginx.conf` para `/etc/nginx/sites-available/carrinho-compras`, `sudo nginx -t` e `sudo systemctl reload nginx` — nenhum workflow aplica este ficheiro.
- [x] **Médio** (corrigido em 31/08/2026) — `PATCH /api/admin/users/:id` (`backend/src/modules/users/users.controller.ts:41-49`) deixava um ADMIN mudar o email de outro utilizador **sem repor `emailVerified: false`**. É o mesmo vetor corrigido a 30/08 em `auth.controller.updateMe` (primeiro achado de 30/08 abaixo), mas a correção não foi aplicada ao caminho de administração: a conta fica com um endereço por confirmar marcado como confirmado e, como `compras.service.ts:43-45` associa faturas por email desde que `emailVerified: true`, passava a receber as faturas desse endereço. Severidade limitada por exigir ADMIN, mas era uma inconsistência com a correção já aplicada ao caminho self-service. **Correção:** o caminho de administração passou a repor `emailVerified: false` e a enviar o email de verificação para o endereço novo, tal como o `PATCH /api/auth/me`. 3 testes novos em `users.test.ts`.
- [x] **Médio** (corrigido em 31/08/2026) — mudar o **próprio** email (`PATCH /api/auth/me`, `auth.controller.ts:87-96`) não exigia a password atual, ao contrário de mudar a password (que exige, via `updateMeSchema.refine`). Quem apanhasse um JWT válido (ex: XSS, ver o achado dos cabeçalhos acima) podia trocar o email para um seu e depois usar `/forgot-password` nesse endereço para tomar a conta — o `emailVerified: false` não trava, porque a reposição de password não exige email confirmado. **Correção:** `updateMe` passou a exigir e validar `currentPassword` sempre que o email muda (400 se faltar ou estiver errada), e essa validação corre **antes** da verificação de email duplicado — o 409 "Email já em uso" revela que um endereço tem conta, e deixou de ser alcançável por quem não sabe a password da própria. A validação é feita uma só vez mesmo quando o pedido muda email e password ao mesmo tempo. No frontend, o campo "Password atual" aparece na secção "Informação pessoal" do `/perfil` assim que o email escrito difere do atual. 4 testes novos em `auth.test.ts` e um `ProfileView.test.ts` novo (5 testes) — a view não tinha ficheiro de testes.
- [x] **Médio** (corrigido em 31/08/2026) — `POST /api/compras` não era idempotente. Não havia restrição única sobre o número da fatura (só ia para o texto de `notes`), por isso uma repetição do workflow n8n (timeout seguido de retry) voltava a criar todos os `PriceRecord` da mesma fatura. **Correção:** `PriceRecord` ganhou `invoiceRef` e `invoiceLine` (migration `20260831112000_add_invoice_idempotency`), preenchidos só pela ingestão e `NULL` nos registos criados à mão. `compras.service.ts` verifica, dentro da transação, se já existem preços com aquele `invoiceRef` naquele supermercado: se sim devolve os registos da importação original com `alreadyImported: true` e contadores a 0, sem criar nada. Como rede de segurança para dois pedidos iguais em paralelo (que passariam ambos na verificação), há um `@@unique([invoiceRef, supermarketId, invoiceLine])` — o segundo aborta com P2002 → 409 em vez de duplicar. `invoiceLine` faz parte da chave para uma fatura poder repetir a mesma linha (dois artigos iguais lidos em separado); o Postgres não aplica o índice único a linhas com `NULL`, por isso os registos manuais não são afetados. 3 testes novos.
- [x] **Baixo** (corrigido em 31/08/2026) — `deploy.yml:76` escrevia o `.env` da VM com `echo "${{ secrets.ENV_FILE }}"` dentro de aspas duplas no shell remoto: qualquer `$` ou backtick numa das variáveis (`POSTGRES_PASSWORD`, `JWT_SECRET`, `N8N_API_KEY`, `RESEND_API_KEY`) era expandido/substituído pelo bash antes de chegar ao ficheiro, corrompendo o segredo em silêncio. Confirmado num shell local: com `echo "…"`, `` `whoami` `` corre mesmo, `$$` vira o PID e `$abc` desaparece; com heredoc citado tudo se mantém literal. **Correção:** `cat <<'ENV_EOF' > .env`, em `deploy.yml` e `deploy-staging.yml`.
  - ⚠️ **Consequência a ter em conta no primeiro deploy depois desta correção**: se o secret `ENV_FILE` contiver mesmo `$` ou crase, o `.env` na VM passa a ficar com o valor *verdadeiro*, diferente do que lá estava. Um `JWT_SECRET` afetado invalida as sessões abertas (toda a gente tem de voltar a entrar); um `POSTGRES_PASSWORD` afetado é pior — o container Postgres foi inicializado com o valor corrompido, por isso a app deixaria de conseguir ligar-se. Convém verificar o secret antes. (O `.env` local deste checkout não tem nenhum destes caracteres.)
- [x] **Baixo** (corrigido em 31/08/2026) — o deploy corria `npx prisma migrate deploy` sem fazer backup imediatamente antes; o único backup era o cron diário das 03:15. Como não há rollback automático (decisão documentada no `CLAUDE.md`), uma migração destrutiva feita a meio do dia podia custar até ~24h de dados. **Correção:** `deploy.yml` corre `./scripts/backup-db.sh` antes do `migrate deploy`. O `docker compose up -d` devolve assim que os containers arrancam e não quando o Postgres já aceita ligações, por isso há uma espera com `pg_isready` (20 × 3s) antes do dump — senão o backup falhava por corrida e, com `set -e`, rebentava o deploy. Se o Postgres não responder de todo, o backup é saltado com aviso (não há nada para salvaguardar e a migração ia falhar a seguir de qualquer forma); já um dump que arranque e falhe aborta o deploy de propósito. `deploy-staging.yml` não leva backup: os dados são descartáveis e o `backup-db.sh` faz dump da BD de produção, não da de staging.
- [x] **Baixo** (corrigido em 31/08/2026) — `comprasSchemaRequest` (`shared/src/schemas/compras.schema.ts`) validava o *formato* da data (`/^\d{2}\/\d{2}\/\d{4}$/`) mas não a sua validade: `32/13/2026` passava e produzia um `Invalid Date` em `compras.service.ts`, que o Prisma rejeitava com **500** em vez de 400. **Correção:** `.refine()` que reconstrói a data em UTC e confirma que o dia/mês/ano sobrevivem à normalização (rejeita `31/02`, aceita `29/02` só em anos bissextos). Acrescentados também `.max(500)` no array `produtos` e limites de comprimento em `fatura` (120), `local` (200) e `produto` (300). 4 testes novos.
- [ ] **Baixo/informativo** — não há revogação de JWT: os tokens são stateless com validade de 7 dias, por isso repor a password (ou um ADMIN alterá-la) não expulsa sessões já abertas. Mitigação habitual: campo `tokenVersion` no `User`, incluído no payload e verificado no `requireAuth`.

### Documentação desatualizada detetada nesta auditoria

- `CLAUDE.md` diz que só `/api/auth/login` e `/register` têm rate limit. Na verdade `verify-email` (10/min), `resend-verification` (3/5min), `forgot-password` (3/5min) e `reset-password` (5/min) também têm — ou seja, os endpoints que enviam email já estão protegidos contra email bombing.
- `CLAUDE.md` diz que `/docs/json` "continua acessível em qualquer ambiente". É verdade ao nível do Fastify, mas em produção o nginx só encaminha `/api/*`: `/docs` e `/docs/json` devolvem o `index.html` da SPA (verificado, ambos 200 com HTML). A spec OpenAPI **não** está exposta publicamente.

### Sem achados / verificado nesta auditoria

- `npm audit`: as mesmas 3 vulnerabilidades "high" em `deepmerge-ts` via CLI do Prisma (achado de 30/08, ainda aberto abaixo). Sem alterações.
- Nenhum segredo versionado — `.env` no `.gitignore`, só `backend/.env.example` no repo; nenhum literal de credencial no código (só fixtures de teste).
- Zero `v-html`, `innerHTML`, `eval` ou `new Function` no código do frontend; nenhum `target="_blank"`.
- Tokens de verificação/reposição: 32 bytes aleatórios, guardados só como hash SHA-256, com TTL (24h/1h) e uso único (anulados ao serem consumidos).
- FKs de autoria (`createdById`/`updatedById`) confirmadas na BD como `ON DELETE SET NULL` — eliminar um utilizador **não** apaga o histórico de preços dele (os registos ficam sem dono e passam a ser editáveis só por ADMIN, via `canWriteResource`).
- `updateUserSchema` não expõe `theme` nem `emailVerified` à administração; os `select` de utilizadores não devolvem `password` nem os hashes dos tokens.
- `npm run lint` sem erros; 88 testes de backend + 74 de frontend a passar; `vue-tsc --noEmit` e `tsc --noEmit` do backend sem erros.
- **Falso alarme, registado para não se repetir o diagnóstico:** correr `npx tsc --noEmit -p backend/tsconfig.json` **a partir da raiz do repo** dá 3 erros `TS2883` (`inferred type ... cannot be named without a reference to 'PrismaPg'`) em `users.service.ts`. Corrido de dentro de `backend/` passa sem erros — é um artefacto da resolução de caminhos relativos do `node_modules`, não um problema de tipos.

## 30/08/2026

- [x] **Alto** (corrigido em 30/08/2026 12:39) — `PATCH /api/auth/me` (`backend/src/modules/auth/auth.controller.ts:82-100`) deixa mudar o `email` sem repor `emailVerified: false` nem exigir reverificação. Como `compras.service.ts` faz find-or-create de `User` por email para associar faturas do n8n, um utilizador pode mudar o seu email para o de outra pessoa e sequestrar os registos de preços dela. **Correção aplicada:** `updateMe` repõe `emailVerified: false` e reenvia o email de verificação para o novo endereço quando o email muda; adicionalmente, `compras.service.ts` só associa a fatura a um utilizador existente se `emailVerified: true` (caso contrário cai no utilizador Sistema), fechando o vetor mesmo antes da reverificação. Testes novos em `auth.test.ts` (3) e `compras.test.ts` (1).
- [ ] **Alto** — `npm audit` (root e `backend`) reporta 3 vulnerabilidades "high" em `deepmerge-ts`, arrastadas pela CLI do Prisma (`@prisma/config`, devDependency). Downgrade do Prisma 7→6 resolveria mas contraria a stack atual — aguardar patch upstream, reavaliar periodicamente.
- [x] **Médio** (corrigido em 30/08/2026) — faltava `@fastify/helmet` em `backend/src/app.ts`: sem cabeçalhos de segurança HTTP (`X-Content-Type-Options`, CSP/`X-Frame-Options`, `Referrer-Policy`). **Correção:** registado `@fastify/helmet` com a CSP predefinida (estrita) em produção; desligada só fora de produção para não partir a Swagger UI em `/docs` (que de qualquer forma não existe em produção).
- [x] **Baixo** (corrigido em 30/08/2026) — `backend/src/shared/middleware/apiKey.middleware.ts:11` comparava o `N8N_API_KEY` com `!==` em vez de `crypto.timingSafeEqual`. **Correção:** comparação agora em tempo constante (`timingSafeEqual`, com verificação de comprimento primeiro). Testes novos em `compras.test.ts` para key errada do mesmo comprimento e de comprimento diferente.
- [x] **Falso positivo, corrigido no registo em 30/08/2026** — a auditoria original tinha assinalado que `requireAuth` (não `requireAdmin`) bastaria para editar/eliminar produtos e preços de outros utilizadores. Ao investigar para corrigir (o utilizador confirmou que só o criador ou ADMIN deveria poder editar/eliminar), descobriu-se que **a restrição já existe**: `canWriteResource` (`backend/src/shared/lib/ownership.ts`, introduzido no commit `732f666`, bem antes desta auditoria) já bloqueia com 403 quem não é o criador nem ADMIN, aplicado em `products`, `variants`, `prices` e `supermarkets` — com testes a cobrir o 403 em `products.test.ts`. A auditoria original só tinha olhado para o `onRequest: [requireAuth]` das rotas, sem verificar a lógica dentro dos controllers. Nenhuma alteração de código foi necessária.
- [x] **Limpeza** (feita em 30/08/2026) — existia um `git stash` local (não era código no repo, só nota) de uma migração SQLite→Postgres já concluída e mergeada em `main`. Descartado (`git stash drop`) com confirmação do utilizador.
- [x] **Médio** (corrigido em 30/08/2026, commits `5c6e419` e `4689517`) — `GET /products` (`backend/src/modules/products/products.service.ts:26`) tem `take: 500` sem paginação na API. `CompareView.vue` e `PricesView.vue` chamavam `productsApi.getAll()` sem filtro para preencher `<select>`s de produto, ficando sujeitos a esse corte silencioso: produtos a partir do 501º (ordem alfabética) simplesmente não apareciam nesses seletores. Reportado pelo utilizador como "não estão a ser carregados todos os produtos" em Comparar. **Correção aplicada:** os `<select>` de produto em `CompareView.vue` (seletor de produto) e `PricesView.vue` (filtro da listagem + formulário de criar/editar) foram substituídos por comboboxes de pesquisa com debounce que chamam `productsApi.getAll({ search })`, contornando o `take: 500` porque a pesquisa filtra no backend antes do corte. Testes atualizados em `CompareView.test.ts`.
  - [x] **Corrigido em 30/08/2026, commit `73f8676`** — `GET /products` passou a devolver `{ data, total }` paginado (`limit`/`offset`, por omissão `limit: 500`/`offset: 0` — mesmo comportamento de antes para quem não pagina), tal como `/prices` já fazia. `ProductsView.vue` (o catálogo) usa agora paginação real (`PAGE_SIZE = 20`, botões Anterior/Seguinte) em vez de carregar tudo de uma vez; o filtro por categoria e a pesquisa por nome repõem a página a 0. O deep-link `/produtos/:id/editar` deixou de depender do produto estar na página carregada — passou a ir buscar o produto diretamente por `productsApi.getById`. Todos os outros consumidores de `productsApi.getAll()` (combobox de mover variante em `ProductsView.vue`, `ReviewProductsView.vue`, `PricesView.vue`, `CompareView.vue`) foram atualizados para o novo formato de resposta (`{ data, total }`). Testado manualmente com 33 produtos locais (paginação, pesquisa, filtro, deep-link). Testes de backend (`products.test.ts`) e frontend atualizados/novos.

## 30/08/2026 (achados detetados fora da auditoria inicial, via logs de produção)

- [x] **Alto** (corrigido em 30/08/2026) — `GET`/`PATCH /api/auth/me` devolviam sempre **500** em produção (`ResponseSerializationError: createdAt esperado como string, recebido "date"`). `profileResponseSchema` exige `createdAt` como string, mas o Prisma devolve sempre um `Date` real — nunca serializa sozinho. Detetado nos logs do deploy falhado de `72df5db` (2026-08-29). **Correção:** `getProfile`/`updateProfile` em `auth.service.ts` serializam agora `createdAt` para ISO string antes de devolver. Os testes existentes mascaravam o bug porque os mocks usavam `.toISOString()` diretamente (uma string) em vez de um `Date` real como o Prisma devolve — corrigido para usar `Date` reais nos mocks, com uma asserção nova a confirmar a serialização.
- [ ] **A investigar** — o deploy de `72df5db` (2026-08-29) falhou no CI com `Error: listen EADDRINUSE: address already in use 0.0.0.0:3000` ao reiniciar o pm2, apesar do script já fazer `pm2 delete all` antes de arrancar (ver comentário em `deploy.yml`). O deploy seguinte (`fddbde0`, 2026-08-30) correu sem problemas — pode ter sido um processo órfão pontual na VM (ex: um restart manual anterior a correr fora do pm2). Vale a pena confirmar na VM (`sudo lsof -i :3000` ou `ss -ltnp | grep 3000`) se não há nenhum processo a correr fora do pm2 antes de assumir que foi só um caso isolado.

### Sem achados / decisões deliberadas confirmadas nesta auditoria

`npm run lint`, `vue-tsc --noEmit` e `npm test` (78 backend + 63 frontend) passavam todos nesta data. `role` como String simples, 409 no registo, backups locais, `EMAIL_FROM` de teste e o fallback de `JWT_SECRET` em dev são decisões já documentadas no `CLAUDE.md` — não repetidas aqui.
