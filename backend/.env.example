DATABASE_URL=
POSTGRES_PASSWORD=
PORT=
NODE_ENV=development
JWT_SECRET=
JWT_EXPIRES_IN=
N8N_API_KEY=
# Bucket GCS para backups diários da BD (scripts/backup-db.sh). Deixar vazio desativa os backups.
BACKUP_GCS_BUCKET=